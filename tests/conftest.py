# conftest.py — shared pytest configuration
# No shared fixtures needed currently since each test file
# manages its own temp files. Add fixtures here as the test
# suite grows (e.g. a shared temp git repo for bisector tests).
